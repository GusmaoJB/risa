% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risaplot.R
\name{risaplot}
\alias{risaplot}
\title{Plot RISA spatial outputs}
\usage{
risaplot(x, area_of_interest = TRUE, raw_ggplot = FALSE)
}
\arguments{
\item{x}{An object of class `risaMaps` or `risaHRA`.

  For `risaMaps` objects, `x` must contain species and stressor kernel
  density maps, usually stored as `species_kernel_maps` and
  `stressor_kernel_maps`. It may also contain species-stressor overlap maps
  in `overlap_maps` and an area-of-interest polygon in `area_of_interest`.

  For `risaHRA` objects, `x` must contain risk outputs generated by the RISA
  habitat risk assessment workflow, including total raw risk, reclassified
  risk layers, and, when available, species-level, stressor-level, and
  ecosystem-level risk maps.}

\item{area_of_interest}{Logical. Should the area of interest be added to the
maps when available? If `TRUE`, the area of interest stored in `x` is
overlaid as a transparent polygon. If `FALSE`, no area-of-interest layer is
added. Defaults to `TRUE`.}

\item{raw_ggplot}{Logical. If `FALSE`, the function applies default themes,
titles, and fill scales. If `TRUE`, the function returns minimally styled
`ggplot` objects without the default RISA theme, titles, or fill scales,
allowing users to customize the plots manually. Defaults to `FALSE`.}
}
\value{
A list of `ggplot` objects. The number and type of plots returned
  depend on the class and internal structure of `x`.
}
\description{
Generates `ggplot2` maps from RISA output objects, including objects of
class `risaMaps` and `risaHRA`. The function automatically detects whether
the spatial outputs are raster-based (`SpatRaster`) or vector-based (`sf`)
and returns a consistent set of maps for species or habitats, stressors,
overlaps, and risk outputs.
}
\details{
The function supports both raster and vector outputs.

Raster layers are converted to data frames using `terra::as.data.frame()`
and plotted with `ggplot2::geom_tile()`. Vector layers are combined into
grouped `sf` objects and plotted with `ggplot2::geom_sf()`.

Reclassified risk values are converted to ordered factor levels:
`"None"`, `"Low"`, `"Medium"`, and `"High"`.

If `area_of_interest = TRUE` and an area of interest is available in `x`, it
is added to the plots as a transparent polygon. If no explicit area of
interest is available for raster outputs, the function may use the spatial
extent of the raster data as an invisible reference layer to preserve map
extent.

If `raw_ggplot = TRUE`, the function omits default titles, themes, and fill
scales. This is useful when users want to fully customize the resulting
plots using additional `ggplot2` layers.
}
\section{Output for `risaMaps` objects}{

When `x` is a `risaMaps` object, the function returns three plots:
\itemize{
  \item \strong{Species KDE:} Kernel density maps representing species
    occurrence, probability of use, or spatial intensity.
  \item \strong{Stressor KDE:} Kernel density maps representing stressor
    spatial distribution or exposure intensity.
  \item \strong{Species-stressor overlaps:} Maps showing the spatial
    co-occurrence between species and stressors.
}

For raster-based outputs, values are plotted with `geom_tile()`. For
vector-based outputs, polygons are plotted with `geom_sf()`. Layers are
faceted by species, stressor, or species-stressor combination.
}

\section{Output for `risaHRA` objects}{

When `x` is a `risaHRA` object, the function returns risk maps according to
the hierarchical depth of the object.

For species-level HRA outputs, the returned plots may include:
\itemize{
  \item raw stressor-specific risk maps;
  \item reclassified stressor-specific risk maps;
  \item highest stressor risk maps;
  \item total combined risk maps;
  \item reclassified total combined risk maps.
}

For ecosystem-level HRA outputs, the returned plots may include:
\itemize{
  \item raw and reclassified species-level risk maps;
  \item raw and reclassified stressor-specific risk maps faceted by species
    and stressor;
  \item raw and reclassified ecosystem-level risk maps.
}
}

\section{Performance}{

The function uses vectorized list operations and fast row-binding routines
to combine multiple raster or vector layers before plotting, avoiding
repeated iterative `rbind()` calls where possible.
}

\examples{
\dontrun{
# Plot outputs from a risaMaps object
plots <- risaplot(risa_maps)
plots[[1]]

# Plot outputs from a risaHRA object
risk_plots <- risaplot(hra)
patchwork::wrap_plots(risk_plots)

# Return minimally styled ggplot objects for further customization
raw_plots <- risaplot(hra, raw_ggplot = TRUE)
raw_plots[[1]] +
  ggplot2::theme_minimal() +
  ggplot2::labs(title = "Customized risk map")

# Suppress the area-of-interest layer
plots_no_aoi <- risaplot(hra, area_of_interest = FALSE)
}

}
\seealso{
`risa_prep()`, `quick_hra()`, `quick_byra()`
}
