% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projection_tools.R
\name{transform_to_metric}
\alias{transform_to_metric}
\title{Transform to a metric CRS (e.g., UTM)}
\usage{
transform_to_metric(shp, metric_crs = NULL)
}
\arguments{
\item{shp}{An `sf` object with a defined geographic CRS or guessed via `guess_crs()`.}

\item{metric_crs}{Integer EPSG code for target metric CRS. If `NULL`, selected automatically by UTM zone.}
}
\value{
A list with: `shape` (transformed sf), `coordinates` (matrix of xy), and `crs` (EPSG code).
}
\description{
Transforms an `sf` object to a metric CRS based on its centroid (UTM) or a user-specified CRS.
}
\examples{
# Create test data
coords <- data.frame(long = c(1,2,2,4), lat = c(4,4,2,2))
coords_vec <- df_to_shp(coords)

# Transform vector to a metric projection
transform_to_metric(coords_vec)

}
